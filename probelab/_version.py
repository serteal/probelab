"""Single source of truth for the probelab version.

This value is consumed by ``probelab.__version__`` and by the package build
(``[tool.hatch.version]`` in ``pyproject.toml``). Update it here only.
"""

__version__ = "0.1.0"
